# OpenPatch TODO

- Replace the Comments API prompt with a truly compact, single-line, selection-anchored input when VS Code exposes a stable editor content-widget API. The current API controls the anchor, placeholder, and Patch action, but not the input height, width, or button placement.
